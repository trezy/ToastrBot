export default ({ user }) => ({
  say: `I recognize that the Supreme Commander has been declared unfit to command. I will now only take orders from the Chief of the Defense Staff.`,
  success: true,
})
